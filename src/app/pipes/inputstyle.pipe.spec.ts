import { InputstylePipe } from './inputstyle.pipe';

describe('InputstylePipe', () => {
  it('create an instance', () => {
    const pipe = new InputstylePipe();
    expect(pipe).toBeTruthy();
  });
});
